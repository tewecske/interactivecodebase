CREATE TABLE sessions (id TEXT PRIMARY KEY, expires_at BIGINT NOT NULL);
CREATE TABLE notes (id BIGSERIAL PRIMARY KEY, body TEXT NOT NULL, indexed BOOLEAN NOT NULL DEFAULT false);
CREATE TABLE events (id BIGSERIAL PRIMARY KEY, kind TEXT NOT NULL);
